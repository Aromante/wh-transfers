# Cloudflare Pages (frontend) envs
# Point to the Supabase Edge Function
VITE_API_BASE=https://<project-ref>.supabase.co/functions/v1/transfers

# API Key for authenticated endpoints (must match SHOPIFY_API_KEY_TRANSFERS in Supabase)
VITE_API_KEY=

